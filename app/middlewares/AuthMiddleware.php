<?php

class AuthMiddleware
{
    // public static function handle()
    // {
    //     if (!isset($_SESSION['user_id']) && !in_array($_SERVER['REQUEST_URI'], ['/auth/login', '/auth/register'])) {
    //         header('Location: /auth/login');
    //         exit;
    //     }
    // }
}